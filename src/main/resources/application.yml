spring:
  datasource:
    url: jdbc:sqlite:./data/database.sqlite
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  blacklite:
    path: ./data/database.sqlite
